{
  "name": "bmi-kalkulator",
  "version": "1.0.0",
  "description": "Aplikasi Kalkulator BMI menggunakan Flask",
  "scripts": {
    "build": "python -m pip install -r requirements.txt",
    "start": "python app.py"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}